// Flutter imports:
import 'package:flutter/material.dart';

typedef Json = Map<String, dynamic>;

class LocaleConst {
  static const locales = [Locale('en', 'US'), Locale('th', 'TH')];

  static const localePath = 'assets/translations';
}
